import React from 'react';

const Comparables = () => {
    return (
        <div>
            <h1>Peer Comparison Metrics</h1>
            {/* Add logic here to display comparison metrics */}
        </div>
    );
};

export default Comparables;
